---
description: Jump to the session that most needs attention
---
Run `zdev next` (Bash). Report which session it switched to and why it ranked
first (dead > permission > oldest decision > finished). If nothing needs
attention, say so.
