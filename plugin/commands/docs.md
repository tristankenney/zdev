---
description: Open the zdev user guide
---
Run `zdev docs` (Bash) to open the local guide in the browser. If the user
asked a specific question, also answer it directly — the guide's content
covers: the attention-layer model, workspace-as-registry discovery rules,
the initiative lifecycle, sidebar glyphs, keys, notification tiers, landing
readiness, and config knobs.
