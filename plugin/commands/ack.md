---
description: Acknowledge statuses (like mark-all-read)
argument-hint: "[--all | <project>]"
---
Run `zdev ack $ARGUMENTS` (default `--all` when no argument given). Confirm
what was acknowledged. New agent activity re-raises attention normally.
