---
description: Diagnose the zdev setup
argument-hint: "[--probe]"
---
Run `zdev doctor $ARGUMENTS` (Bash; `--probe` runs the live end-to-end test).
Explain any failures found and fix what is safely fixable, asking first for
anything destructive.
