---
description: Mute or unmute zdev notifications
argument-hint: "[mute [30m|1h|forever] | unmute | status]"
---
Run `zdev notify $ARGUMENTS` (default: status). The mute gates EVERYTHING the
daemon fires — banner and spoken announcements both. Report the resulting
state and, when muting, when it expires.
